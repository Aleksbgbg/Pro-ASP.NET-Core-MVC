@model ProductsList

@foreach (Product product in Model.Products)
{
    <div>
        <h3>@product.Name</h3>
        @product.Description
        <h4>@product.Price.ToString("C")</h4>
    </div>
}

<div page-action="List" page-model="@Model.PagingInfo"></div>